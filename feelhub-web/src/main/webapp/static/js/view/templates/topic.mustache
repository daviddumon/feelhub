{{#url}}
    <a href="{{url}}" class="topic {{classes}}" id="{{topicId}}">
{{/url}}
{{^url}}
    <div class="topic {{classes}}" id="{{topicId}}">
{{/url}}
    <img src="{{illustration}}" onload="OnImageLoad(event);"/>
    <span class="{{sentimentValue}}">{{name}}</span>
{{#url}}
    </a>
{{/url}}
{{^url}}
    </div>
{{/url}}